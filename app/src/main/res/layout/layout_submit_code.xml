<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background_color"
    android:fitsSystemWindows="true">

    <include
        android:id="@+id/title_activity_submit"
        layout="@layout/title_base" />


    <EditText
        android:id="@+id/activity_code"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@+id/title_activity_submit"
        android:layout_gravity="center_horizontal"
        android:layout_margin="20dp"
        android:background="@drawable/feed_back_edit_shape"
        android:gravity="top"
        android:hint="请输入验货码"
        android:maxLength="300"
        android:singleLine="false"
        android:textColor="@color/black_color"
        android:textColorHint="@color/car_message_text_color" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/activity_code"
        android:orientation="vertical"
        android:visibility="gone"
        android:weightSum="2">

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/separate_color" />

        <ImageView
            android:id="@+id/iv_submit_code"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_margin="20dp"
            android:layout_weight="1"
            android:src="@mipmap/icon_upload_code" />
    </LinearLayout>


    <Button
        android:id="@+id/btn_submit"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center_horizontal"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="40dp"
        android:background="@drawable/button_selector"
        android:text="提   交"
        android:textColor="@color/white_color"
        android:textSize="17sp" />
</RelativeLayout>